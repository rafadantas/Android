<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.rafa.escolafacil">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Escola Fácil"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.ui.activity.LoginActivity">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />

                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.ui.activity.ListaEscolasActivity" />
        <activity android:name=".ui.ui.activity.RegisterActivity" />
        <activity android:name=".ui.ui.activity.ForgotPassActivity" />

    </application>

</manifest>